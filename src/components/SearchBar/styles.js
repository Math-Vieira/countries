import styled from "styled-components";

export const SearchBar = styled.div`
    width: 100%;
    display: flex;
    justify-content: space-between;
    padding: 2.5rem 0;
    flex-wrap: wrap;
    gap: 10px;
`